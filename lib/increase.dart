import 'package:flutter/material.dart';

class IncreasePage extends StatelessWidget {
  const IncreasePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
